import './API.js';

$(function() {
  $('#slider').slider({min: 10, startValueThumbOne: 10}, 'update');

  // $('#slider').slider({min: 20, startValueThumbOne: 0}, 'update');
  // пофиксить, почему то начальное знаяение рассчитывается не правильно если они похожи
});

$(function() {
  $('#sliders').slider({min: 10, startValueThumbOne: 10}, 'update');

  // $('#slider').slider({min: 20, startValueThumbOne: 0}, 'update');
  // пофиксить, почему то начальное знаяение рассчитывается не правильно если они похожи
});
 // осмортеть видос про jquery плагины
